{
  "name": "legocity-weather-server",
  "version": "1.0.0",
  "description": "Weather data server with auto-update to NGSI-LD Context Broker",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "elysia": "^1.0.0",
    "@elysiajs/swagger": "^1.0.0",
    "@elysiajs/cors": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
