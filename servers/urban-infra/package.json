{
  "name": "urban-infra",
  "version": "1.0.0",
  "description": "Server quản lý dữ liệu hạ tầng kỹ thuật đô thị (NGSI-LD)",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "elysia": "^1.0.0",
    "@elysiajs/swagger": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
